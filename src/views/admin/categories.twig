{{ use('bizley/podium/client/widgets/sortable') }}
{{ set(this, 'title', Yii.t('podium.client.header', 'admin.categories')) }}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="#">Home</a></li>
        <li class="breadcrumb-item"><a href="#">Library</a></li>
        <li class="breadcrumb-item active" aria-current="page">Data</li>
    </ol>
</nav>

<h1 class="mb-3">
    <a href="{{ path(['admin/add-category']) }}" class="btn btn-primary float-right">
        {{ FA('plus') }} {{ Yii.t('podium.client.button', 'category.add') }}
    </a>
    {{ FA('sitemap') }} {{ Yii.t('podium.client.header', 'admin.categories') }}
</h1>

{{ sortable_begin() }}
    {% for category in categories %}
        {{ include('categories-item.twig') }}
    {% endfor %}
{{ sortable_end() }}
